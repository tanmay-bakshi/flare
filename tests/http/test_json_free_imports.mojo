"""Compile smoke for HTTP and WebSocket clients without community JSON."""

from flare.http import HttpClient
from flare.ws import WsClient


def main():
    print("HttpClient and WsClient import without community JSON")
